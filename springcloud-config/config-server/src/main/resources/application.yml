server:
  port: 8888

spring:
  application:
    name: config-server
    
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chenjiecg/SpringCloud/
          searchPaths: springcloud-config/config-repo
          username: 
          password: 
      label: master
      
