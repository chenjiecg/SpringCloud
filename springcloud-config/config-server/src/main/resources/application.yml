server:
  port: 8888

spring:
  application:
    name: config-server
    
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chenjiecg/SpringCloud/
          searchPaths: respo
          username: 
          password: 
      label: master